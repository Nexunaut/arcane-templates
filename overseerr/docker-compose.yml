services:
  overseerr:
    image: sctx/overseerr:latest
    container_name: overseerr
    environment:
      - LOG_LEVEL=info
      - TZ=${TZ}
    volumes:
      - "/mnt/ssd/overseerr:/app/config"
    ports:
      - "5055:5055"
    restart: unless-stopped
